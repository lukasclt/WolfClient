plugins {
    id 'java'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(8)
    }
}

group 'com.exemplo'
version '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    // Dependências podem ser adicionadas aqui
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

tasks.jar {
    manifest {
        attributes(
            'Main-Class': 'com.exemplo.client.WolfClient'
        )
    }
}